
import tests from '../tests.js';

/**
 * @param {import('../../../unit/unit.js').Utility}
 */
export default ({ assert, describe, it }) => {
    describe('#tests', () => {
        describe('', () => {

        });
    });
};

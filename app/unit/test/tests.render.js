
import {

} from '../render.js';

/**
 * @param {import('../state.js').Utility}
 */
export default ({ assert, describe, it }) => {
    describe('_animateDots', () => {
        it('should render a dot for each result onto the `dotsContainer`', () => {
            // let dotsContainer = document.createElement('div');
            // _animateDots(dotsContainer, [ { msg: 'hi', ok: true } ]).then(() => {

            //     console.log(dotsContainer);
            //     assert(dotsContainer.querySelector('.ok')).isObject();
            // });
        });
    });
};
